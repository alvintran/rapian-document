{% extends 'partials/site.html.twig' %}

{% block banner %}
	<section class="section-home-banner">
		<div class="home-banner-holder">
			<div class="home-background">
				<img src="http://rapian.com/assets/images/poster.jpg" alt="Background">
			</div>
		</div>
		<div class="home_header">
			<div class="hero_container">
				<div class="container hero_content text-center">
					<h1 class="slogan">
						<p class="head-slogan">{{ site.title }}</p>
						<div class="hr"></div>
						Tài liệu hướng dẫn giúp bạn quản lý hiệu quả với Rapian
					</h1>
					<a target="_blank" href="http://rapian.com/auth/dang-ky.html?utm_source=top_navigation&amp;utm_medium=cpc&amp;utm_campaign=rapian_help" class="rap-button rap-button-medium rap-button-free" style="float: none;">Tạo website ngay</a>
				</div>
			</div>
		</div>
	</section>
{% endblock %}

{% block content %}
	<section id="PageContainer" class="is-moved-by-drawer">
		<div class="container">
			<div class="page-block">
				<div class="page-title">
					<div class="auto-box clearfix">
						<h1 class="col-lg-12 animated fadeIn in text-center" data-animation="fadeIn" data-transition="1500"> Chào mừng bạn đến với Rapian</h1>
					</div>
				</div>

				{% macro loop(page) %}
					{% for p in page.children %}
						{% if p.visible %} {% set current_page = (p.active or p.activeChild) ? 'active' : '' %}
							{% if p.children.count > 0 %}
								<article class="col-lg-4 col-md-4 col-sm-6 col-xs-12 animated fadeInUp in col">
									<h4><a href="{{ p.url }}" title="{{ p.menu }}">{{ p.menu }}</a></h4>
									<ul class="list-unstyled">
										{% for key, sub in  p.children|slice(0, 5) %}
											{% set body_classes = sub.header.body_classes ? sub.header.body_classes : 'fa-file-text-o' %}
											<li>
												<a href="{{ sub.url }}" title="{{ sub.menu }}">
													<i class="fa {{ body_classes  }}" aria-hidden="true"></i>
													{{ sub.menu }}
												</a>
											</li>
										{% endfor %}
									</ul>
								</article>
							{% endif %}
						{% endif %}
					{% endfor %}
				{% endmacro %}

				<section class="topic-listing all">
					<div class="auto-box">
						<div class="row clearfix row-eq-height">
							<div class="listing-container">
								{{ _self.loop(pages) }}
							</div>
						</div>
					</div>
				</section>
			</div>
		</div>
	</section>
{% endblock %}